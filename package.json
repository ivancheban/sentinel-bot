{
  "name": "telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot for group verification",
  "main": "netlify/functions/bot.js",
  "dependencies": {
    "telegraf": "^4.12.2"
  },
  "scripts": {
    "build": "echo 'Build step completed' && npm install"
  }
}